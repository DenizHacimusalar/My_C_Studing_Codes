#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,i;
    printf("enter a random number:");
    scanf("%d",&a);
    for(i=1;i<=a;i++){
        for(j=i;j<=i;j++)
    }
    return 0;
}
